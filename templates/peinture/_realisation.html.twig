

<div class="card mb-6 shadow-light-lg lift lift-lg">

    <a href="{{ path('detail-realisation',{'slug': peinture.slug}) }}" class="card-img-top">
        <img src="{{ vich_uploader_asset(peinture ,'imageFile')}}" alt="" class="card-img-top">
    </a>
        <a href="{{ path('detail-realisation',{'slug': peinture.slug}) }}" class="card-body">

            <h3>{{ peinture.nom }}</h3>
            <p class="mb-0 text-muted">
                {{ peinture.description|u.truncate(200 ,'...' , false) }}
            </p>
    </a>
    <a href="{{ path('detail-realisation',{'slug': peinture.slug}) }}" class="card-meta mt-auto">
        <hr class="card-meta-divider">
        <p class="h6 text-uppercase text-muted mb-0 ms-auto">
            <time> Ajoutée le {{ peinture.createdAt|date('d/m/y') }}</time>
        </p>

    </a>
</div>



